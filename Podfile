# Uncomment the next line to define a global platform for your project
platform :ios, '10.1'

target 'PhotoBrowser500px' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 4.0'

  target 'PhotoBrowser500pxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotoBrowser500pxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
